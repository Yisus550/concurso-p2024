@model IEnumerable<concurso_p2024.Models.Materia>
@{
    ViewData["Title"] = "Lista de Materias";
    Layout = "/Views/Shared/_Layout.cshtml";
}

<h1> Lista de materias</h1>

<div class="container">
    <div class="row mt-5">
        <div class="col-3">
            <a class="btn btn-primary text-white form-control" asp-action="Crear">Crear Materia</a>
        </div>
    </div>
    <div class="row mt-5">
        <div class="col-12">
            @if (Model.Count() > 0)
            {
                <table class="table table-striped border">
                    <thead>
                        <tr>
                            <th>Nombre</th>
                            <th>Creditos</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <th>@item.Nombre</th>
                                <td>@item.Creditos</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <h2>No hay ninguan materia registrada</h2>

            }
        </div>
    </div>
</div>